#include <stdio.h>
#include <stdlib.h>
int main(){
    char S[1000],T[100];

    int i,j;

    for(i = 0; i<len(T) ; i++){
        for(j = 0; j<len(S) ; j++){
            if(T[i] == S[j] && j  ){
                continue;
            }
        }
    }
}